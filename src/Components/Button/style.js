import styled from '@emotion/styled'
import { Button } from '@mui/material'

export const GenButton = styled(Button)({
  width: '100%',
  height: '10%',
  padding: '10px',
  backgroundColor: '#436850',
  color: '#FEFBF6',
  '&:hover': { backgroundColor: '#12372A' },
})
